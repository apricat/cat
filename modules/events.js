  var Events = (function () {

      function listener( cat ) {

        // trigger certain events at milestones with cats

      }

    	function init() {}

    	return { 

    		init : init,
        listener : listener
    	};
  }());

  Events.init();
